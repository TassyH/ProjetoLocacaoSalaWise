<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.locacaodesalas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Ui.TelaVisualizaUsuario"></activity>
        <activity android:name=".Ui.CadastroReservaActivity" />
        <activity android:name=".Ui.ReservaSalaActivity" />
        <activity android:name=".Ui.ListaSalasActivity" />
        <activity android:name=".Ui.CadastroUsuarioActivity" />
        <activity android:name=".Ui.LoginUsuarioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>